{
  "name": "gunak-web-app2",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:6001",
  "homepage": "/app",
  "dependencies": {
    "prop-types": "^15.6.2",
    "ra-data-json-server": "^2.4.2",
    "react": "^16.7.0",
    "react-admin": "^2.6.2",
    "react-dom": "^16.7.0",
    "react-scripts": "2.1.3",
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "chai": "^4.2.0"
  },
  "scripts": {
    "clean": "rm -rf node_modules && rm package-lock.json && rm -rf build",
    "start-nhsrc": "HTTPS=true PORT=3001 REACT_APP_TENANT=NHSRC react-scripts start",
    "start-jss": "PORT=3002 REACT_APP_TENANT=JSS react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy-local": "yarn build && rm -rf ../facilities-assessment-server/app/* && cp -r build/* ../facilities-assessment-server/app/",
    "deploy-igunatmac-qa": "ssh sam@139.59.19.108 \"rm -rf /home/app/qa-server/facilities-assessment-host/app-servers/app/*\" && scp -r build/* sam@139.59.19.108:/home/app/qa-server/facilities-assessment-host/app-servers/app/",
    "build-deploy-igunatmac-qa": "npm run build && yarn deploy-igunatmac-qa",
    "deploy-nhsrc-qa": "ssh gunak-other \"rm -rf /home/app/qa-server/facilities-assessment-host/app-servers/app/*\" && scp -r build/* gunak-other:/home/app/qa-server/facilities-assessment-host/app-servers/app/",
    "build-deploy-nhsrc-qa": "npm run build && yarn deploy-nhsrc-qa"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
